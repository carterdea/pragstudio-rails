- if object.errors.any?
  %section.errors
    %h3 Oops! The #{object.class.name.titleize.downcase} couldn't be saved
    %p Please correct the following #{pluralize(object.errors.count, 'error')}:
    %ul
      - object.errors.full_messages.each do |message|
        %li= message
