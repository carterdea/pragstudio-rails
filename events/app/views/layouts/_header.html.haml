%header.page-header
  %h1= link_to "Pragmatic Events", root_path
  %nav
    %p
      - if current_user
        = link_to current_user.name, current_user
      - else
        = link_to "Sign Up", new_user_path
        = link_to "Sign In", new_session_path
