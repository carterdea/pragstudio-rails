%article
  %header
    %h2= @event.name
  %p= @event.description
  %table
    %tr
      %th When:
      %td= @event.starts_at
    %tr
      %th Where:
      %td= @event.location
    %tr
      %th Price:
      %td= format_price(@event)
=link_to "All Events", events_path
| 
=link_to "Edit", edit_event_path(@event)